{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Fortran (No Debug)",
      "type": "node",
      "request": "launch",
      "name": "Run Fortran",
      "runtimeExecutable": "${fileDirname}/${fileBasenameNoExtension}.exe",
      "console": "externalTerminal",
      "preLaunchTask": "compile-fortran"
    }
  ]
}
